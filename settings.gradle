pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

def cellwatchProperties = new Properties()
file("cellwatch.properties").withInputStream { cellwatchProperties.load(it) }
def mapboxToken = cellwatchProperties.getProperty("MAPBOX_DOWNLOADS_TOKEN")

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven { url 'https://jitpack.io' }
        maven {
            url 'https://api.mapbox.com/downloads/v2/releases/maven'
            authentication {
                basic(BasicAuthentication)
            }
            credentials {
                // This should always be `mapbox` (not your username).
                username = 'mapbox'
                password = mapboxToken
            }
        }
    }
}
rootProject.name = "cellwatch"
include ':app'
